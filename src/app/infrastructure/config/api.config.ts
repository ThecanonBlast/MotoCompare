// URL base del backend (MotoCompareBackend). Sin `environments` configurados
// todavía — cuando se necesite distinguir dev/producción, esto se mueve ahí
// sin que el resto de la app (repositorios HTTP) tenga que cambiar nada.
export const API_BASE_URL = 'http://localhost:3002/api';
